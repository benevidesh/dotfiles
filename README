benevidesh's dotfiles
---------------------

Uses GNU Stow – https://www.gnu.org/software/stow/

Usage 1: 
cd ~
git clone https://github.com/benevidesh/dotfiles.git
stow dotfiles

Usage 2 (more fine-grained):
cd ~
git clone https://github.com/benevidesh/dotfiles.git
cd dotfiles/.config/
stow -t $HOME/.config/nvim nvim (zathura, tmux, etc.) /* make sure the destination folder already exists */
